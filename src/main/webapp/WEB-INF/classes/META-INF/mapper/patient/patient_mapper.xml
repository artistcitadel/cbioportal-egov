<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="patientcpm">

    <select id="selectDigClssifyHrc" parameterType="map" resultType="hashmap">
      SELECT DISTINCT EXAM_CD id,
                EXAM_ENG_NM name
      FROM CLSPNEXRT
      <![CDATA[
        WHERE LENGTH(EXAM_CD) < 6
      ]]>
    </select>

    <select id="selectDigClssify" parameterType="map" resultType="hashmap">
     SELECT DISTINCT A.EXAM_SLIP_CD pid ,
                          A.EXAM_CD id,
                          A.EXAM_ENG_NM name,
                          A.RCEP_DT time
        FROM CLSPNEXRT A
        WHERE A.RESCH_PAT_ID = #{patientId}
        ORDER BY 1,4
        LIMIT  10
 	</select>

    <select id="selectTestSpec" parameterType="map" resultType="hashmap">
    SELECT    'LAB_TEST' subject,
                EXAM_CD id ,
                EXAM_ENG_NM name,
                EXAM_RSLT_VAL exam,
                MARK_RSLT_VAL mark,
                CRTE_MSR_VAL crte,
                RCEP_DT time
                FROM CLSPNEXRT
                WHERE RESCH_PAT_ID = #{patientId}
                AND LENGTH(EXAM_CD) = '6'
                /*and exam_cd in('BB0013','BC0001')*/
                ORDER BY 2, 6
                LIMIT  5
    </select>
</mapper>


