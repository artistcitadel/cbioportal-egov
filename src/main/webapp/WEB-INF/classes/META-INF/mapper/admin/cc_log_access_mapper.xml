<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC  
 "-//mybatis.org//DTD Mapper 3.0//EN"  
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
 
 
 <mapper namespace="cc_log_access">
	<insert id="insertLogAccess" parameterType="map">
		INSERT INTO CC_LOG_ACCESS 
		(                           
			TYPE,                   /* 로그종류 */
			REQUEST_URL,            /* 요청URL */
			PER_CODE,               /* 사용자 ID */
			ACCESS_IP,              /* 접근 IP */
			MESSAGE,
			CRT_DT                 /* 일자 */
		)
		VALUES
		(
			#{TYPE},
			#{REQUEST_URL},
			#{PER_CODE},
			#{ACCESS_IP},
			#{MESSAGE},
			NOW() 					/* LOAD_DT      */
		)
	
	</insert>

</mapper>
 
 
 