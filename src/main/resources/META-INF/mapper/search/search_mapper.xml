<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC  
 "-//mybatis.org//DTD Mapper 3.0//EN"  
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
 
 <!-- 개인자료 저장 -->
 <mapper namespace="search">
	<select id="selectSynonymList" parameterType="map" resultType="hashmap">
		SELECT  REP.SEQ AS REP_SEQ,
				REP.TERM AS REP_TERM,
				REP.LANG AS REP_LANG,
				SYN.SEQ AS SYN_SEQ,
				SYN.TERM AS SYN_TERM 
		FROM	CC_SYNONYM REP, CC_SYNONYM SYN
		WHERE 1=1
		AND REP.SEQ = SYN.REP_SEQ
		AND REP.TERM = #{SEARCH_VAL}
		AND REP.REP_SEQ=0
		AND REP.REP_YN = 'Y'
		AND REP.DELETE_YN = 'N'
	
	</select>
	<select id="selectPatSbstNoList" parameterType="map" resultType="hashmap">
		${SEARCH_PAT_SBST_NO_LIST}
	
	</select>

</mapper>
 
 
 